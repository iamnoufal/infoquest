const { default: Layout } = require("components/Layout")

const RegisterPage = () => {
  return (
    <Layout>
      <h1>This is for the register page</h1>
    </Layout>
  )
}

export default RegisterPage